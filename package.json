{
  "name": "photodrop",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["backend", "frontend", "common"],
  "description": "A Progressive Web App for privately sharing photos within isolated groups",
  "scripts": {
    "setup:dev": "./scripts/setup.sh dev",
    "setup:prod": "./scripts/setup.sh prod",
    "deploy:prod": "./scripts/deploy.sh",
    "teardown": "./scripts/teardown.sh all",
    "teardown:dev": "./scripts/teardown.sh dev",
    "teardown:prod": "./scripts/teardown.sh prod",
    "dev": "echo 'Run: cd backend && npm run dev (in one terminal) and cd frontend && npm run dev (in another)'",
    "secrets:scan": "nix run .#secrets-scan",
    "create-group": "./scripts/create-group.sh",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "format": "prettier --check \"e2e/**/*.ts\"",
    "format:fix": "prettier --write \"e2e/**/*.ts\""
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "prettier": "3.7.4"
  }
}
